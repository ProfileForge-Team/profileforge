# Frontend talks only to API Gateway, not to individual services.
VITE_API_BASE_URL=http://localhost:18000

# false uses the real backend. Set true for local demo data in localStorage.
VITE_USE_MOCK_API=false
