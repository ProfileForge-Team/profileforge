# URL API Gateway. Фронтенд обращается только к нему, не к отдельным микросервисам.
VITE_API_BASE_URL=http://localhost:8000
# true — демонстрационные данные в localStorage; false — реальные запросы к API Gateway.
VITE_USE_MOCK_API=true
